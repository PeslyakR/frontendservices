export enum ERolesActiveTypes {
  FINDALL = '[Roles]-get-all',
  FIND_BY_ID = '[Roles]-find-by-id',
  CREATE = '[Roles]-create',
  DELETE = '[Roles]-delete',
  UPDATE = '[Roles]-update',
  SUCCESS_GET_ONE = '[Roles]-get-one-success',
  SUCCESS_GET_MANY = '[Roles]-get-many-success',
  SUCCESS_DELETE = '[Roles]-delete-success',
  ERROR = '[Roles]-failure',
}
